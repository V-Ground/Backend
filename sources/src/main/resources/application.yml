spring:
  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:v-cloud-lab
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
      # show_sql: true
      # format_sql: true

  # frontend:
  # ip: localhost:3000

jwt:
  secret: 12345678901234567890123456789012

frontend:
  ip: 54.180.120.49:3000 # localhost:3000 # 3.34.45.79

# 파일 내용 보기 & bash history array 아니라 문자열 하나로 받기